server:
  port: 8888
spring:
  application:
    name: ms-administracion-server-config
    
    
# Configuracion Local
  profiles:
    active: native
  cloud:
    config:
      server: 
        native:
          searchLocations: file:///D:/Anderson/Curso Galaxy-Java/Practica/ms-administracion-data-config

          
          
          
# Acuator
management:
  endpoints:
    web:
      exposure:
        include: "health,refresh"
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true 